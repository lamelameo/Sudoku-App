	    final LayoutInflater inflater2 = ((Activity) mContext).getLayoutInflater();
            View testView = inflater.inflate(R.layout.activity_grid_test, parent, false);

            // radiobuttons
            CustomRadioButton rb1 = testView.findViewById(R.id.Set1);
            CustomRadioButton rb2 = testView.findViewById(R.id.Set2);
            CustomRadioButton rb3 = testView.findViewById(R.id.Set3);
            CustomRadioButton rb4 = testView.findViewById(R.id.Set4);
            CustomRadioButton rb5 = testView.findViewById(R.id.Set5);
            CustomRadioButton rb6 = testView.findViewById(R.id.Set6);
            CustomRadioButton rb7 = testView.findViewById(R.id.Set7);
            CustomRadioButton rb8 = testView.findViewById(R.id.Set8);
            CustomRadioButton rb9 = testView.findViewById(R.id.Set9);

            final List<CustomRadioButton> rbList = new ArrayList<>(Arrays.asList(rb1, rb2, rb3, rb4, rb5, rb6, rb7, rb8, rb9));

            sudokuButton = convertView.findViewById(R.id.sudokuButton);
            sudokuButton.setTextOn(mStringIds[position]);
            //sudokuButton.setId(position);

//            Log.i(TAG, "getView: 2"+sudokuButton);
//            Log.i(TAG, "getView: 3"+rb1);

            sudokuButton.setOnClickListener(new View.OnClickListener() {
                @Override
                public void onClick(View v) {
                    // check if any radiobutton is on - only change state if one is on - else do nothing
//                    CustomRadioButton ahhh = rbList.get(0);
//                    boolean thing1 = ahhh.isChecked();
//                    Log.i(TAG, "testRB2->" +thing1)


                    for (CustomRadioButton element : rbList) {
                        // if the radio button isnt on...
                        if (!element.isChecked()) {
                            Log.i(TAG, "TEST1");
                            ; //do nothing
                        } else {
                            Log.i(TAG, "TEST2");
                            // set the clicked cells value to the corresponding integer
                            int pos = rbList.indexOf(element);
                            //int pos = Arrays.asList(radioButtons).indexOf(element);  // 1-9
                            String integerString = Integer.toString(pos);
                            sudokuButton.setTextOn(integerString);
                            sudokuButton.setChecked(true);
                        }
                    }
                }
            });